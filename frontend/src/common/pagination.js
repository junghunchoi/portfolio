

export const handleUpdateSize = (params, value) => {
  params.size = value;
  params.start = 1;
}

export const handleUpdateOrder = (params, value) => {
  params.order = value;
}

export const handleUpdateSort = (params, value) => {
  params.sort = value;
}